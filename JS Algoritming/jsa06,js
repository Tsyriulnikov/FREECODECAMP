//Confirm the Ending
// function confirmEnding(str, target) {
//     return str.endsWith(target);
//   }
  
//   confirmEnding("Bastian", "n");

function confirmEnding(str, target) {
    return str.indexOf(target,(str.length-target.length)) != -1  ? true : false ; }
  
    console.log(confirmEnding("Bastian", "an"));